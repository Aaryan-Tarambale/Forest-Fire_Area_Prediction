# Forest-Fire_Area_Prediction
A machine learning project to predict forest fire burned area using the UCI dataset. Models like Random Forest and Gradient Boosting were tuned and evaluated. The project analyzes skewed data, interprets feature importance, and highlights challenges in predicting large fires.
